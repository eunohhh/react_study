import { Outlet } from "react-router-dom";

const Detail = () => {
    return (
        <div>
            야야야야야야임마
            <br />
            자식아
            <br />
            <Outlet></Outlet>
        </div>
    );
};

export default Detail;
